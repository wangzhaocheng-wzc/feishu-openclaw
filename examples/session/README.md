# SESSION 示例

这些文件是 `SESSION.md` 的场景化示例，方便 wik 直接复制到仓库根目录作为当前任务的现场指挥板。

- `SESSION.article-writing.example.md`：写文章
- `SESSION.material-collection.example.md`：收集材料
- `SESSION.skill-learning.example.md`：学习/接入新技能

用法：

1) 复制某个示例为仓库根目录的 `SESSION.md`
2) 按实际任务填空，并每 20-30 分钟更新一次
